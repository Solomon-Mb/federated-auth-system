# federated-auth-system
A web-based Federated Identity Management system that allows users to sign in using Google (OAuth 2.0). The application acts as a service provider and does not store local usernames or passwords.
